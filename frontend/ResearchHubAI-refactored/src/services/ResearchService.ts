/**
 * ResearchService
 *
 * Architecture placeholder for backend integration (see TASK 14).
 * Intentionally left unimplemented — no business logic, no fake data,
 * no fetch calls. Wire these methods up to api/client.ts once the
 * ASP.NET Core backend endpoints are available (see api/endpoints.ts).
 */
export class ResearchService {
  // TODO: implement once backend is available
}

export const researchService = new ResearchService();
