import AdminRouter from "../routes/AdminRouter";

/** Layout wrapper for the Administrator role. See StudentLayout for the pattern. */
export default function AdminLayout() {
  return <AdminRouter />;
}
